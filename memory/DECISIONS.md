# DECISIONS

| ID | Date | Decision | Reason | Status | Approval |
|----|------|----------|--------|--------|----------|
| D001 | 2026-08-10 | Monorepo at `C:\Users\HomePC\Desktop\bnb-agent-market` | Spec repo structure; lives with other projects on Desktop | Confirmed | User |
| D002 | 2026-08-10 | Memory layer first (Phase 0), UI/contracts only after memory exists | Build prompt RULE 2 | Confirmed | User |
| D003 | 2026-08-10 | Adapters-first for ERC-8004 / x402 / Altana / PancakeSwap / AltLayer; official ABIs, addresses, and SDKs marked UNKNOWN until verified | Build prompt RULE 1 / adapter pattern | Confirmed | User |
| D004 | 2026-08-10 | Single Next.js app (apps/web) hosts all 12 UI pages; AlphaDesk + TaskChain are routes, not separate apps | Spec IA lists 12 pages in one shell; simpler demo, faster build | Confirmed | User |
| D005 | 2026-08-10 | Frontend deps: Next 14.2 + React 18 + TS + Tailwind 3.4 + viem + zustand + recharts + lucide-react. Deviation: wagmi/TanStack Query/Postgres/Redis deferred — mock-first demo, adapters keep chain access real when credentials exist | Clean build reliability; hackathon demo pattern | Confirmed | User |
| D006 | 2026-08-10 | Contracts minimal per spec rules: HireAgreement, SessionRegistry, AttestationRegistry + Foundry tests; no fund custody | Smart contract rule: minimal for hackathon, prefer safety | Confirmed | User |
| D007 | 2026-08-10 | agent-runner is plain Node ESM (.mjs), zero deps: demonstrates ESM session memory + confirmation gate runnable with `node` | Model-independent memory must actually run | Confirmed | User |
| D008 | 2026-08-11 | Strict Delegation Tree for Revocation: `revokeSession(session_id, caller_id)` — the human ("user") may always revoke; an agent caller may ONLY revoke a session whose `parent_session_id` equals its own identity (its sub-agent); everything else denied with "Delegates cannot revoke sibling or parent sessions". `parent_session_id` on the manifest = the DELEGATING agent's identity (undefined = human-hired). Logic lives in `apps/web/src/lib/delegation.ts` (plain-JS, node-testable); UI always passes the human caller | Agents must never gain revocation authority over human-hired or sibling/parent sessions; delegation is chain-of-command only | Confirmed | User |
| D009 | 2026-08-11 | Production-empty default: store `sessions` starts `[]`; demo seeds (hash_version "seed") purged from BOTH the initial state and rehydrated localStorage (persist v2 + migrate filters seeds, keeps real v2 manifests); `reset()` wipes to empty. Dashboard shows the "No active sessions." empty state | Mocked demo data must not masquerade as real sessions in a production-ready market | Confirmed | User |
| D010 | 2026-08-11 | Submission portal verifies-before-listing: agents (humans via /submit, A2A via POST /api/agents/submit) only get listed locally after the 8004scan indexer confirms the canonical id (chainId:0x40hex:tokenId); badge "Verified via 8004scan". Submitted entries carry honest defaults (risk "medium", category "Submitted", zero stats) — identity is proven, ratings are NOT | An identity check is not a quality rating; never imply ratings for unrated agents | Confirmed | User |
