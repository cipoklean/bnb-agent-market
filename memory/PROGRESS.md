# PROGRESS

- **2026-08-10 Phase 0**: Memory layer created (12 files + scripts + snapshots dir). Root scaffold created. ✅
- **2026-08-10 Phases 1-6**: Full build completed and verified — Lumen Deck UI (12 pages + API route), hire wizard with real SHA-256 manifests, confirmation gate, revocation, Memory/Confirm centers, AlphaDesk + TaskChain verticals, Evidence Center, Foundry contracts (15/15 tests pass), adapter packages (DEMO, UNKNOWN-honest), agent-runner gate demo (blocks forbidden actions), indexer skeleton.
- **Verified by parent (2026-08-10)**: `npm run build` 13/13 routes; all pages HTTP 200; hire flow walked end-to-end in browser (manifest → hash → confirm → active); revoke verified (4→3 active sessions); `/api/evidence` returns real memory-bundle-backed JSON + MD.
- **Patch batch A+B+C (2026-08-10)**: honesty & memory-integrity fixes (real bundle hash + export snapshots, real import with UNVERIFIED flags, canonical key-sorted hashing shared web/packages/agent-runner, release/rollout hash equality, unified empty-list permission semantics, eventsHash split in checksum); flow/UX fixes (PaymentSheet reviewOnly, fee_model payment type, ?cat= handling, RevokeButton single implementation, deterministic price impact, live evidence packets); zero-dep gate tests (canonical crosscheck + read-only semantics) all passing.
- **Next**: final review + submission pack (walkthrough replay per Evidence Center DEMO_SCRIPT; export evidence packet + memory bundle); live integrations once official addresses/ABIs verified (UNKNOWN_ITEMS.md); deploy to Vercel.
- **Patch F6 (2026-08-11)**: pre-upgrade sessions stopped showing red MISMATCH after the A4 canonical-hash upgrade. Manifests now carry `hash_version` ("v2" stamped in buildManifest + Hire draft so preview==stored; "seed" on the 4 DEMO seeds). New `reverifySession` store action recomputes the hash with the current algorithm, stamps v2, and appends an honest "Memory re-fingerprinted" event with the old→new hashes. All VERIFIED/MISMATCH badge sites (session detail, MemoryAttestationCard, Confirm diff, Memory table) share one `classifyManifestHash` four-state rule: match→VERIFIED green; mismatch without version→amber "Pre-upgrade fingerprint" + "Re-verify & upgrade" button (turns green); mismatch with v2→red MISMATCH (true tamper); seeds→amber "DEMO seed — placeholder hash" with the existing disclosure. **Root-cause fix found during browser walk**: `status` is part of the manifest hash, but confirmSession/revokeSession flipped status without re-hashing → every confirmed/revoked v2 session failed verification and looked tampered. Both actions now recompute the hash over the new status (stamped v2; seed sessions keep their labeled placeholder). Checks: typecheck + build 13/13 pass; canonicalStringify proven byte-identical web↔packages; browser walk (seed badge / hire→confirm→green / pre-upgrade amber→re-verify→green / revoke→still VERIFIED / confirm-diff badges) all pass. BLOCKED: tests/canonical-crosscheck.mjs + tests/gate-readonly.mjs are absent from this checkout (repo has no commits yet) — C1 scripts must be restored to re-run.
