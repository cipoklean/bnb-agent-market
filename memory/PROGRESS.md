# PROGRESS

- **2026-08-10 Phase 0**: Memory layer created (12 files + scripts + snapshots dir). Root scaffold created. ✅
- **2026-08-10 Phases 1-6**: Full build completed and verified — Lumen Deck UI (12 pages + API route), hire wizard with real SHA-256 manifests, confirmation gate, revocation, Memory/Confirm centers, AlphaDesk + TaskChain verticals, Evidence Center, Foundry contracts (15/15 tests pass), adapter packages (DEMO, UNKNOWN-honest), agent-runner gate demo (blocks forbidden actions), indexer skeleton.
- **Verified by parent (2026-08-10)**: `npm run build` 13/13 routes; all pages HTTP 200; hire flow walked end-to-end in browser (manifest → hash → confirm → active); revoke verified (4→3 active sessions); `/api/evidence` returns real memory-bundle-backed JSON + MD.
- **Patch batch A+B+C (2026-08-10)**: honesty & memory-integrity fixes (real bundle hash + export snapshots, real import with UNVERIFIED flags, canonical key-sorted hashing shared web/packages/agent-runner, release/rollout hash equality, unified empty-list permission semantics, eventsHash split in checksum); flow/UX fixes (PaymentSheet reviewOnly, fee_model payment type, ?cat= handling, RevokeButton single implementation, deterministic price impact, live evidence packets); zero-dep gate tests (canonical crosscheck + read-only semantics) all passing.
- **Patch F6 (2026-08-11)**: pre-upgrade sessions stopped showing red MISMATCH after the A4 canonical-hash upgrade. Manifests now carry `hash_version` ("v2" stamped in buildManifest + Hire draft so preview==stored; "seed" on the 4 DEMO seeds). New `reverifySession` store action recomputes the hash with the current algorithm, stamps v2, and appends an honest "Memory re-fingerprinted" event with the old→new hashes. All VERIFIED/MISMATCH badge sites (session detail, MemoryAttestationCard, Confirm diff, Memory table) share one `classifyManifestHash` four-state rule: match→VERIFIED green; mismatch without version→amber "Pre-upgrade fingerprint" + "Re-verify & upgrade" button (turns green); mismatch with v2→red MISMATCH (true tamper); seeds→amber "DEMO seed — placeholder hash" with the existing disclosure. **Root-cause fix found during browser walk**: `status` is part of the manifest hash, but confirmSession/revokeSession flipped status without re-hashing → every confirmed/revoked v2 session failed verification and looked tampered. Both actions now recompute the hash over the new status (stamped v2; seed sessions keep their labeled placeholder). Checks: typecheck + build 13/13 pass; canonicalStringify proven byte-identical web↔packages; browser walk (seed badge / hire→confirm→green / pre-upgrade amber→re-verify→green / revoke→still VERIFIED / confirm-diff badges) all pass. BLOCKED: tests/canonical-crosscheck.mjs + tests/gate-readonly.mjs are absent from this checkout (repo has no commits yet) — C1 scripts must be restored to re-run.
- **Mainnet Bridge Phase 1 — Research & Discovery (2026-08-11)**: all 5 UNKNOWN integrations researched against official docs, SDKs, and live mainnet. ERC-8004 → KNOWN (Identity Registry `0x8004A169FB4a3325136EB29fA0ceB6D2e539a432` RPC-verified on BSC mainnet; Reputation `0x8004BAa17C55a88189AE136b182e5fdA19dE9b63`; official bnbagent SDK registration flow `register_agent()`; Validation Registry NOT PUBLISHED). Binance x402/B402 → KNOWN (X-PAYMENT-REQUIREMENTS / open PAYMENT-REQUIRED+PAYMENT-SIGNATURE headers, EIP-712 TransferWithAuthorization + Permit2 permitWitnessTransferFrom payloads, /verify+/settle facilitator flow, gas-sponsored direct token transfer; facilitator EOA + Permit2 proxy addresses NOT PUBLISHED — runtime /supported). Altana → KNOWN (grantSession/revokeSession with on-chain spend caps + expiry; BNB mainnet KeyStore `0x6572427ED530BadcF7375Cf9A4709D8d2b0E7E0a` + KeyStoreController `0x0834Ee2C9BdC3E3efF0a2dC34393D4B0e546A555`). PancakeSwap V3 → KNOWN (BSC mainnet SwapRouter `0x1b81D678ffb9C0263b24A97847620C99d213eB14`, NonfungiblePositionManager `0x46A15B0b27311cedF172AB29E4f4766fbE7F4364`, QuoterV2 `0xB048Bbc1Ee6b733FFfCFb9e9CeF7375518e25997` + full set verbatim from official repo). AltLayer → KNOWN (public 8004scan REST API `https://8004scan.io/api/v1/public`, OpenAPI spec, per-agent metrics; AltLLM API still UNKNOWN). Sources: eips.ethereum.org/EIPS/eip-8004, github.com/bnb-chain/bnbagent-sdk, github.com/erc-8004/erc-8004-contracts, 8004scan.io (live), developers.binance.com onchainpay-x402, x402-foundation/x402 spec, docs.altana.network (llms-full), pancakeswap/pancake-v3-contracts deployments/bscMainnet.json, developer.pancakeswap.finance. Dossiers: docs/research/*.md. Memory updated: INTEGRATIONS.md (KNOWN + exact values), UNKNOWN_ITEMS.md (9 remaining items). No code changes (research + memory only, per role).
- **Mainnet Bridge Phase 2 — COMPLETE both legs (2026-08-11)**: testnet dry-run then mainnet LIVE. Testnet (97): agentId 1798, tx `0xa1ae43b6…8e1d0`, IdentityRegistry `0x8004A818BFB912233c491871b3d84c89A494BD9e`; safety gate exercised (env RPC mainnet → canonical testnet fallback after chainId check). **Mainnet (56): agentId 263312, tx `0xd4715ce1…650c71` (success, block 115236467, gasUsed 622,584), IdentityRegistry `0x8004A169FB4a3325136EB29fA0ceB6D2e539a432`, wallet `0x3C8176953eadBeE7b9bF8C6a5d1CF1153D924E11`, self-paid (~0.0083 BNB balance; run explicitly approved by user).** Both indexed by 8004scan in real time: testnet `97:0x8004a818…:1798` (is_testnet true), mainnet `56:0x8004a169fb4a3325136eb29fa0ceb6d2e539a432:263312` (is_testnet false, x402_supported true, created_tx_hash matches receipt, UI https://8004scan.io/agents/bsc/263312). Scores 0/health null on both = honest fresh state; post-launch usage metrics trackable per-agent on the public API. Evidence: `docs/submission/evidence/agent-registration.md` (both networks, verbatim mainnet JSON), `8004scan-verification.json` + `-testnet.json`, `last-registration.json` + `-testnet.json`. Scripts: `indexer/scripts/register-agent.mjs` / `verify-agent.mjs` (zero new deps).
- **Mainnet Bridge Phase 3 — ERC-8004/8004scan slice (2026-08-11)**: real live metrics in the UI. erc8004 adapter DEMO→KNOWN with `getLiveScanMetrics()` (fetch + merge for mainnet agent, graceful console.warn→mock fallback), same-origin proxy `/api/8004scan/[chainId]/[tokenId]` (IP-rotation probe: first A-record blackholed from this network; node:https+SNI per-IP 2.5s caps; numeric guard; force-dynamic no-store), "Live Mainnet Metrics" panel on /agents/portfolio-reporter (real health/score/feedbacks, green x402 check, View on 8004scan, honest badge + fresh-agent note). Browser-verified: panel live w/ real payload (0s are the true fresh-agent state), other agents untouched/mock, marketplace deterministic, fallback proven by first failing direct fetch. typecheck clean; build 14/14 (marketplace/settings stay static); evidence `/api/8004scan/abc/263312`→400. Evidence: docs/submission/evidence/8004scan-ui-integration.md. Remaining Phase-3 slices (x402/Altana/PancakeSwap clients) still TODO — no new deps without user OK.
- **Next**: Mainnet Bridge Phase 4 — decide B402 onboarding vs self-facilitator for x402; AltLLM telemetry; then finish remaining adapter swaps (with user OK per adapter). Then final review + submission pack (walkthrough replay, evidence packet + memory bundle export; tests/verify-mainnet-bridge-evidence.mjs covers Phase 2-3 chain+indexer evidence).
